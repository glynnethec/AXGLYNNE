import { notFound } from 'next/navigation';

export default function BlogPage() {
  // This section is reserved for future architecture. 
  // Returning notFound() triggers the 404 page automatically.
  notFound();
}
